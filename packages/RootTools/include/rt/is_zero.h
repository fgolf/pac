#ifndef RT_IS_ZERO_H
#define RT_IS_ZERO_H

namespace rt
{
        bool is_zero(int value);
        bool is_zero(long int value);
        bool is_zero(float value);
        bool is_zero(double value);
        bool is_zero(long double value);
} // namespace rt

#endif // RT_IS_ZERO_H

